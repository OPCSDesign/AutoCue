{
  "name": "autocue-dev",
  "private": true,
  "type": "module",
  "description": "Dev-only: test tooling for AutoCue (the app itself has no dependencies or build step).",
  "scripts": {
    "test": "node test-matcher.mjs && node test-moonshine.mjs"
  },
  "devDependencies": {
    "@huggingface/transformers": "^4.2.0"
  }
}
