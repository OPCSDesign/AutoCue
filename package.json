{
  "name": "tracingboard-dev",
  "private": true,
  "type": "module",
  "description": "Dev-only: test tooling for TracingBoard (the app itself has no dependencies or build step).",
  "scripts": {
    "test": "node test-matcher.mjs && node test-webdav.mjs && node test-moonshine.mjs"
  },
  "devDependencies": {
    "@huggingface/transformers": "^4.2.0"
  }
}
